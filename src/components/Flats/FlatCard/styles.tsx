import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  button: {
    color: theme.palette.primary.main,
  },
  address: {
    marginBottom: 1.5,
  },
  cardActions: {
    paddingTop: 0,
    paddingLeft: 0,
    paddingRight: 0,
    paddingBottom: 0,
    marginLeft: 16,
  },
}));
